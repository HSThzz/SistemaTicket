/**
 * @file Ponto de entrada do kernel compartilhado (enumeradores de domínio).
 * @module shared/kernel
 */

export {
  EventStatus,
  OrderStatus,
  ReservationStatus,
  TicketStatus,
  UserRole,
} from "./enums";
